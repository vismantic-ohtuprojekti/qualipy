import numpy

from imgfilter.utils import *

""" Needs to be fixed
def test_partition_matrix_returns_correct_number_of_partitions():
    mat = numpy.random.randn(100).reshape((10, 10))
    assert len(partition_matrix(mat, 3)) == 10
"""
